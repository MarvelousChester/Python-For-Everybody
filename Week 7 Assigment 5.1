# Write a program which repeatedly reads numbers till the user enters done

i = 0
counter = 0
total = 0
print("Type done when ready")
while i == 0:
    userInput = input("Enter a number:")

    if userInput == "done" or userInput == "Done":
        i += 1  # can use break statement instead of this and change while to Boolean
        break
    try:
        total = total + float(userInput)
        counter += 1
    except:  # Try not to use a bare except often
        print("Invalid Input")
        continue

average = total / counter
print(average)
print("Average number is:" + str(average))
